name = "foundry-ai"
main = "worker.js"
compatibility_date = "2024-01-01"

[vars]
# Non-secret vars can go here. API key is set via: wrangler secret put ANTHROPIC_API_KEY

[[routes]]
# Cloudflare will assign a workers.dev subdomain automatically on first deploy.
# You can also add a custom domain here later.
